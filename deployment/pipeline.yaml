$schema: https://azuremlschemas.azureedge.net/latest/PipelineJob.schema.json
type: pipeline
name: cv_pipeline
display_name: CV Pipeline
version: 9

inputs:
  input_dir:
    type: uri_folder
    description: "Input data directory"

outputs:
  output_dir:
    type: uri_folder
    description: "Output data directory"

jobs:
  validation_step:
    type: command
    component: azureml:cv_model_validate@latest
    inputs:
      input_dir: ${{ inputs.input_dir }}
    outputs:
      output_dir: validation_output

  inference_step:
    type: command
    component: azureml:cv_model_inference@latest
    inputs:
      input_data_path: ${{ jobs.validation_step.outputs.output_dir }}  # Connect validation output
    outputs:
      output_dir: ${{ outputs.output_dir }}